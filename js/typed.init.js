// Typed Text
var typed = new Typed("#typed", {
    strings: ["Web Developer", "Full-Stack Developer", "UI/UX Designer"],
    typeSpeed: 80, // Smooth typing speed
    backSpeed: 40, // Smooth backspacing
    backDelay: 1200, // Pause before erasing
    startDelay: 300, // Small delay before typing starts
    loop: true, // Infinite loop
    showCursor: true, // Show the blinking cursor
    cursorChar: "|", // Cursor symbol
    smartBackspace: true, // Only backspace what’s necessary
});
